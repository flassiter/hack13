# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real credentials.

# AWS credentials for Bedrock access.
# For long-term IAM user keys, leave AWS_SESSION_TOKEN blank.
# For temporary credentials (SSO, assumed role), set all three.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=

# Admin token for protected API endpoints (/api/workflows/*/definition PUT and /explain).
HACK13_API_ADMIN_TOKEN=
